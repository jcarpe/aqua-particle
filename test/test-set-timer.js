var chai = require( 'chai' );
var should = chai.should();
var Timer = require( '../src/utils/set-timer' );

describe( 'Aqua API -- timer utility', () => {

  it( 'should be a valid Timer when instantiated', () => {
    var myTimer = new Timer();
  });

});
